/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "assets-global.website-files.com",
            "www.pixground.com",
            "img.freepik.com",
            "static.dnschecker.org",
            "ucarecdn.com",
            "upload.wikimedia.org",
        ],
    },
};

export default nextConfig;
